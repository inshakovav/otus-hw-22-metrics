management:
  endpoints:
    web:
      exposure:
        include: "*"
#        include: health,prometheus
  metrics:
    export:
      prometheus:
        enabled: true
    distribution:
      percentiles-histogram:
        "[http.server.requests]": true # Для REST-запросов возвращает гистограмму списком: ... le="0.001048576",} 12.0 ...
      slo.http.server.requests: 50ms, 60ms, 100ms, 200ms, 300ms, 400ms # Будут добавлены эти шаг в гистограмму и еще куча других
